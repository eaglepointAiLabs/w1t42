# Backend Module Structure

This directory is reserved for feature modules (feed, training, reviews, moderation, payments, ingestion, analytics).

Only base scaffolding is included in this setup step.
